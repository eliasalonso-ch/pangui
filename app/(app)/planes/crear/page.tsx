export const metadata = { title: "Nuevo plan" };

/**
 * El formulario lo monta el layout, que mantiene vivas las dos hojas (plan y
 * plantilla) para que alternar entre ellas no remonte ni recargue nada. Esta
 * page solo existe para que la ruta siga siendo una ruta real y conserve su
 * título.
 */
export default function NuevoPlanPage() {
  return null;
}
