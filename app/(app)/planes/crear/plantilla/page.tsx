export const metadata = { title: "Plantilla de la orden" };

/**
 * Igual que /planes/crear: la hoja la monta el layout compartido. Ver el
 * comentario de layout.tsx.
 */
export default function PlantillaPage() {
  return null;
}
