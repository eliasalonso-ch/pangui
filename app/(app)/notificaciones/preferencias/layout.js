// Overrides the parent "Notificaciones" title so each tab is distinguishable.
export const metadata = { title: "Preferencias" };

export default function NotificationPrefsLayout({ children }) {
  return children;
}
