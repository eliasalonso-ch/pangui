// Overrides the parent "Notificaciones" title so each tab is distinguishable.
export const metadata = { title: "Reglas de alerta" };

export default function AlertRulesLayout({ children }) {
  return children;
}
