export const metadata = {
  title: "Órdenes de compra | Pangui",
  description: "Órdenes de compra a proveedores: creación, aprobación, envío y recepción.",
};

export default function OrdenesCompraLayout({ children }) {
  return children;
}
