// Título propio para la pantalla de cuadrillas. La página es un componente de
// cliente, así que la metadata tiene que vivir en un layout.
export const metadata = { title: "Cuadrilla" };

export default function CuadrillaLayout({ children }) {
  return children;
}
