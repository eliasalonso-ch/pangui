// Own tab title for the dedicated invite screen. The page is a client
// component, so the metadata has to live in a layout.
export const metadata = { title: "Invitar usuarios" };

export default function InvitarLayout({ children }) {
  return children;
}
