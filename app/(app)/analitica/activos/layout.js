// Titles the tab for this report. A layout is used because the page is a client
// component and cannot export `metadata` itself.
export const metadata = { title: "Analítica de activos" };

export default function AnalyticsActivosLayout({ children }) {
  return children;
}
